<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.bookspace.mvc.model.dao.ReviewDao">
	
	<!-- 리뷰 작성 -->
	<insert id="insertReview">
		INSERT INTO review (
			book_id,
			user_id,
			review_rating,
			review_content
		)
		VALUES(
			#{bookId},
			#{userId},
			#{reviewRating},
			#{reviewContent}
		)
		
	</insert>
	
	
	
	<!-- 리뷰 조회 (by bookId) -->
	<!-- 사용자에게 삭제되지 않은 리뷰들만 보여줘야 함-->
	<select id="selectReviewByBookId" resultType="Review">
		SELECT *
		FROM review
		WHERE book_id = #{bookId}
			AND deleted_at IS NULL
	</select>
	
	
	
	<!-- 리뷰 수정 -->
	<!-- 삭제되지 않은 리뷰에 대해서만 수정 가능-->
	<update id="updateReview">
		UPDATE review
		SET review_rating = #{reviewRating},
			review_content = #{reviewContent},
		WHERE review_id = #{reviewId}
			AND user_id = #{userId}
			AND deleted_at IS NULL
	</update>
	
	
	
	<!-- 리뷰 삭제 (soft delete) -->
	<update id="deleteReview">
		UPDATE review
		SET deleted_at = NOW()
		WHERE review_id = #{reviewId}
			AND user_id = #{userId}
			AND deleted_at IS NULL
	</update>
	
	
	
</mapper>